export const Types = {};

const INITIAL_STATE = {};

export default function Index(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export const Creators = {};
